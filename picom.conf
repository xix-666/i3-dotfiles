backend = "glx";

# Active window opacity
active-opacity = 0.9;

# Inactive window opacity
inactive-opacity = 0.75;

# Frame opacity
frame-opacity = 0.8;

# Enable opacity support
detect-client-opacity = true;

# Optional fading effects
fading = true;
fade-in-step = 0.03;
fade-out-step = 0.03;
